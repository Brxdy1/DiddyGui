-- Gui to Lua
-- Version: 3.2

-- Instances:

local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("ImageLabel")
local DiddyGui = Instance.new("TextLabel")
local weightcounter = Instance.new("TextLabel")

--Properties:

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Frame.Name = "Frame"
Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(98, 36, 206)
Frame.BackgroundTransparency = 1.000
Frame.BorderColor3 = Color3.fromRGB(27, 42, 53)
Frame.Position = UDim2.new(0.546679795, 0, 0.731681943, 0)
Frame.Size = UDim2.new(0, 162, 0, 137)
Frame.Image = "rbxassetid://3570695787"
Frame.ImageColor3 = Color3.fromRGB(81, 81, 81)
Frame.ScaleType = Enum.ScaleType.Slice
Frame.SliceCenter = Rect.new(100, 100, 100, 100)
Frame.SliceScale = 0.120
Frame.Active = true

DiddyGui.Name = "Diddy Gui"
DiddyGui.Parent = Frame
DiddyGui.BackgroundColor3 = Color3.fromRGB(81, 81, 81)
DiddyGui.BorderColor3 = Color3.fromRGB(0, 0, 0)
DiddyGui.BorderSizePixel = 0
DiddyGui.Position = UDim2.new(0, 0, 0.0729926974, 0)
DiddyGui.Size = UDim2.new(0, 162, 0, 50)
DiddyGui.FontFace = Font.new("rbxasset://fonts/families/GrenzeGotisch.json") -- Set custom GrenzeGotisch font
DiddyGui.Text = "Diddy Gui"
DiddyGui.TextColor3 = Color3.fromRGB(0, 0, 0)
DiddyGui.TextSize = 40.000

weightcounter.Name = "weight counter"
weightcounter.Parent = Frame
weightcounter.BackgroundColor3 = Color3.fromRGB(81, 81, 81)
weightcounter.BorderColor3 = Color3.fromRGB(81, 81, 81)
weightcounter.Position = UDim2.new(0.0864197537, 0, 0.554744542, 0)
weightcounter.Size = UDim2.new(0, 133, 0, 49)
weightcounter.FontFace = Font.new("rbxasset://fonts/families/GrenzeGotisch.json") -- Same custom GrenzeGotisch font
weightcounter.Text = ""
weightcounter.TextColor3 = Color3.fromRGB(0, 0, 0)
weightcounter.TextSize = 30.000

-- Scripts:

local function OFLQDT_fake_script() -- weightcounter.LocalScript 
	local script = Instance.new('LocalScript', weightcounter)

	repeat
		wait(.1)
		local num = 0
		for i,v in pairs(game.Players.LocalPlayer.Backpack:GetChildren()) do
			if v.Name == "Double Weight" then
				num += 1
			end
		end
	
		if num == 0 then
			for i,v in pairs(game.Players.LocalPlayer.Character:GetChildren()) do
				if v.Name == "Double Weight" then
					num += 1
				end
			end
		end
		
		script.Parent.Text = num.."" -- Changed Tools.Text to script.Parent.Text
	until num == 9999999999999
end
coroutine.wrap(OFLQDT_fake_script)()

-- Draggable Script:

local UIS = game:GetService("UserInputService")
local dragging
local dragInput
local dragStart
local startPos

local function update(input)
	local delta = input.Position - dragStart
	Frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

Frame.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = true
		dragStart = input.Position
		startPos = Frame.Position
		
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

Frame.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement then
		dragInput = input
	end
end)

UIS.InputChanged:Connect(function(input)
	if input == dragInput and dragging then
		update(input)
	end
end)
